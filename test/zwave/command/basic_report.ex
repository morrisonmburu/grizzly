defmodule ZWave.Command.BasicReportTest do
  use ExUnit.Case, async: true

  describe "creation" do
    test "when all current value is a level value"

    test "when current value is nil"

    test "when current value is invalid"

    test "when current value is a state value"
  end

  test "serialize into binary"

  describe "deserialize from binary" do
    test "when all okay"

    test "when invalid current value"
  end
end
